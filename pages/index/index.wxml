<!--index.wxml-->
<view class="container">
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 微信授权(获得微信头像) </button>
  </view>
  <view>
    <button bindtap="jumpphoto">拍照</button>
  </view>
</view>
